#include <stdio.h>

int main(){
    int numero;

    printf("digite o número: ");
    scanf("%d", &numero);

    if ( numero > 0)
    if (numero % 2 == 0)
    printf("numero par!\n");
    else
    printf("numero impar\n");
    
    if (numero > 0)
    
    printf("positivo!\n");
    else if (numero == 0)
    printf("numero é zero!\n");
    else
    printf("negativo!\n");








}